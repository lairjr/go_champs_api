{
  "swagger": "2.0",
  "paths": {
    "/api/phases/{phaseId}/stats": {
      "post": {
        "tags": [
          "PhaseStat"
        ],
        "summary": "Post phase stats",
        "responses": {
          "201": {
            "schema": {
              "$ref": "#/definitions/PhaseStatResponse"
            },
            "description": "PhaseStat created OK"
          }
        },
        "produces": [
          "application/json"
        ],
        "parameters": [
          {
            "schema": {
              "$ref": "#/definitions/PhaseStatRequest"
            },
            "required": true,
            "name": "tournament_stat",
            "in": "body",
            "description": "The phase stats details"
          }
        ],
        "description": "Creates a new phase stats",
        "consumes": [
          "application/json"
        ]
      },
      "get": {
        "tags": [
          "PhaseStat"
        ],
        "summary": "List phase stats",
        "responses": {
          "200": {
            "schema": {
              "$ref": "#/definitions/PhaseStatsResponse"
            }
          }
        },
        "produces": [
          "application/json"
        ]
      }
    }
  },
  "info": {
    "version": "1.0",
    "title": "Go Champs API Docs"
  },
  "host": "localhost:4000",
  "definitions": {
    "PhaseStatsResponse": {
      "type": "object",
      "title": "PhaseStatsReponse",
      "required": [
        "data"
      ],
      "properties": {
        "data": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PhaseStat"
          },
          "description": "The phase stats details"
        }
      },
      "description": "Response schema for multiple phase stats"
    },
    "PhaseStatResponse": {
      "type": "object",
      "title": "PhaseStatResponse",
      "properties": {
        "data": {
          "description": "The phase stats details",
          "$ref": "#/definitions/PhaseStat"
        }
      },
      "description": "Response schema for single phase stats"
    },
    "PhaseStatRequest": {
      "type": "object",
      "title": "PhaseStatRequest",
      "required": [
        "tournament_stat"
      ],
      "properties": {
        "tournament_stat": {
          "description": "The phase stats details",
          "$ref": "#/definitions/PhaseStat"
        }
      },
      "example": {
        "tournament_stat": {
          "title": "PhaseStat title"
        }
      },
      "description": "POST body for creating a phase stats"
    },
    "PhaseStat": {
      "type": "object",
      "title": "PhaseStat",
      "required": [
        "title"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "PhaseStat ID"
        },
        "title": {
          "type": "string",
          "description": "PhaseStat title"
        },
        "phase_id": {
          "type": "string",
          "description": "Tournament phase ID"
        },
        "inserted_at": {
          "type": "string",
          "format": "datetime",
          "description": "Creation timestamp"
        },
        "updated_at": {
          "type": "string",
          "format": "datetime",
          "description": "Update timestamp"
        }
      },
      "example": {
        "id": "1d8ac55c-fb5c-4605-beaf-30db7d4c2fec",
        "title": "Wins"
      },
      "description": "A phase stats of the app"
    }
  }
}